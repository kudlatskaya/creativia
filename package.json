{
  "homepage": "https://kudlatskaya.github.io/creativia",
  "devDependencies": {
    "gh-pages": "^6.0.0"
  },
  "scripts": {
    "predeploy": "rm -rf build && npm run build",
    "deploy": "gh-pages -d build"
  }
}
